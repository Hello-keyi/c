[project]
name = "c"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "keyi", email = "13692959211.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
c = "c:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
